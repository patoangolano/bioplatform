[lint]
# E402: Module level import not at top of file
# Needed because routers use sys.path manipulation before importing
# mcp_servers adapters and services modules
per-file-ignores = { "apps/api/routers/sequences.py" = ["E402"], "apps/worker/main.py" = ["E402"], "apps/api/routers/regulatory.py" = ["E402"] }

[lint.isort]
known-first-party = ["mcp_servers"]
